<div class="col-12">
  <% flash.each do |type, message| %>
    <div class="alert <%= alert_class_for(type) %>" role='alert'>
      <button class="close" data-dismiss="alert">×</button>
      <%= message %>
    </div>
  <% end %>
</div>
